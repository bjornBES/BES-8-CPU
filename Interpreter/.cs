public static class BaseTypes
{
    static Compiler.Type Char = new(0) { };
    static Compiler.Type Byte = new(0) { };
    static Compiler.Type Word = new(1) { };
    static Compiler.Type UWord = new(2) { };
    static Compiler.Type Int = new(3) { };
}